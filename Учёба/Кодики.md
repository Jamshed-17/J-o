``` c++ 
int main(){
	setlocale (LL_ALL "Res");
	int a, b, c
	cin>>b>>c
```


``` c++

# include

void countKorni (float a, loat b, float c)
{
	float d = b * b - 4 * a * c;
	int k;
	if (d > 0) k = 2;
	else if (d == 0 ) k = 1;
	else k = 0;
	cout<<k;
}

int main(){
	float a, b, c;
	cin>>a>>b>>c;
	countKorni(a, b, c);
	countKorni(b, a, c);
	countKorni(c, b, a);
}
```



