1. Опишите порядок действий для подключения физического Android-устройства по USB
[[1]] -> Подключить кабель
[[2]] -> включить отладку по USB
[[3]] -> подтвердить разрешение на устройстве

2. Что является первым шагом при создании нового эмулятора Android в Android Studio?
Открытие **Device Manager** и нажатие **Create Virtual Device**

3. Какой онлайн-инструмент рекомендуется использовать для генерации гармоничной цветовой палитры в соответствии с Material 3?
Material Theme Builder

4. Какова основная цель использования mutableStateOf в Jetpack Compose при обработке событий?
Хранить изменяемое состояние компонента и автоматически обновлять UI при его изменении.

5. Какой компонент следует использовать для постраничного вывода контента, который перелистывается свайпом влево и вправо?
HorizontalPager

6. Какой модификатор используется для доступа к функциям обработки сложных нажатий, таких как `detectTapGestures`?
pointerInput

7. Архитектура данных и UI для Room DB
Предоставлять чистый API для доступа к данным, инкапсулируя источник данных (DAO, сеть).

8. Какая библиотека является стандартной для реализации Unit-тестов в Android-приложении?
JUnit4

9. Что помогает выявить проблемы адаптивности на раннем этапе разработки в Android Studio?
Использование встроенного UI Check Mode или предпросмотра на разных конфигурациях.

10. Какой класс в примере навигации (NavHost) используется для определения начальной точки приложения и сопоставления UI-элементов (пунктов меню) с экранами?
sealed class NavRoutes

11. Какая функция используется для создания анимации плавного изменения цвета компонента (например, переход от голубого к мадженте)?
animateColorAsState

12. Что обеспечивает **`Companion object`** внутри класса, наследующего `RoomDatabase`, в соответствии с паттерном Singleton?
Гарантирует создание только одного экземпляра базы данных в приложении.

13. Между какими двумя слоями находится Репозиторий в контексте работы с данными (поток данных: DAO → ... → ViewModel)?
DAO и ViewModel

14. Какая потенциальная оптимизация может быть применена к коду, когда приложение собирается в режиме `release`?
Применение обфускации и оптимизации кода (например, с помощью ProGuard/R8).

15. Какая ошибка возникает, если в проекте отсутствует нужная библиотека?
IDE подсвечивает отсутствующие импорты и классы как ошибки

16. Какой файл используется для хранения версий библиотек при работе с каталогом версий?
libs.versions.toml

17. Какая команда добавляется в блок dependencies для использования ViewModel в Compose?
implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.7.0")

18. Какое действие выполняется после изменения зависимостей в Gradle?
Синхронизация проекта (Sync Now)

19. Для чего используется библиотека Room?
Для работы с локальной базой данных

20. Какой тип комментариев в Kotlin позволяет генерировать документацию?
Документирующие (KDoc)

21. Какие комментарии лучше всего подходят для пояснения логики переменных или методов?
Однострочные и блочные

22. Где отображаются KDoc-комментарии в Android Studio?
Во всплывающих подсказках при наведении на класс или метод

23. Каким символом начинаются однострочные комментарии в Kotlin?
//

24. Какой размер экрана рекомендуется для эмулятора в Android Studio?
6–7 дюймов

25. В каком файле проекта, сгенерированном Material Theme Builder, содержится кастомная цветовая палитра для темной и светлой схемы?
Color.kt

26. Какое свойство у компонентов в Compose используется для включения/выключения их интерактивности (кликабельности, возможности ввода)?
enabled

27. За что отвечает функция `rememberPagerState` при создании пагинатора?
Хранит состояние пагинатора и количество страниц

28. Какая функция внутри `pointerInput` используется для одновременной обработки простого, двойного и длительного нажатия?
detectTapGestures

29. Что должен принять в конструкторе класс Репозитория для выполнения операций с базой данных?
Экземпляр DAO (Data Access Object).

30. В каком режиме работает база данных при корректной настройке Unit-тестов для DAO, чтобы не затрагивать реальные данные?
Во временной, in-memory (в оперативной памяти) базе данных.

31. Где в проекте указываются зависимости напрямую?
build.gradle.kts

32. Какая опция отвечает за скорость загрузки эмулятора?
Cold Boot / Quick Boot

33. Что необходимо сделать в MainActivity.kt, чтобы применить кастомную тему ко всему приложению?
Обернуть корневой Composable в компонент `AppTheme`

34. Каким образом связывается текст, введенный пользователем в TextField, с действием, выполняемым по нажатию на Button?
Текст хранится в переменной состояния (mutableStateOf), которая затем используется в блоке onClick кнопки.

35. Каким образом связывается текст, введенный пользователем в TextField, с действием, выполняемым по нажатию на Button?
Текст хранится в переменной состояния (mutableStateOf), которая затем используется в блоке onClick кнопки.

36. Какой объект необходимо создать, чтобы кнопки могли программно перелистывать страницы пагинатора?
CoroutineScope

37. Какой модификатор используется для визуального отображения **масштабирования, вращения и перемещения** компонента после обработки жеста?
Modifier.graphicsLayer

38. Какой механизм используется внутри Репозитория для выполнения операций с БД (вставка, удаление) асинхронно, чтобы не блокировать UI?
CoroutineScope

39. Какая аннотация используется для маркировки функции, которая должна быть выполнена **перед каждым** тестовым методом (например, для настройки БД)?
@Before

40. Что необходимо сделать для включения режима разработчика на физическом Android-устройстве?
Несколько раз нажать на **Номер сборки** в разделе **О телефоне**

41. Какой параметр компонента `Switch` используется для кастомизации содержимого его "бегунка" (thumb), например, для отображения иконки?
thumbContent

42. Если требуется, чтобы кнопка была активна только при условии, что два независимых флажка (Checkbox) отмечены, какая логическая операция должна использоваться для определения свойства enabled кнопки?
Логическое И (&&)

43.  С какого индекса начинается нумерация страниц в блоке контента `{ page -> ... }` пагинатора?
С 0

44. Какая функция хранит состояние мультитач-жестов и принимает лямбды для отслеживания `scaleChange`, `offsetChange` и `rotationChange`?
rememberTransformableState

45. Какой тип данных используется в ViewModel для хранения списка данных из БД, который **автоматически** обновляет UI?
val users: LiveData<List<User>>

46. Что проверяет тестовый метод **`writeAndReadInList()`** при работе с DAO?
Возможность ввода и последующего вывода (поиска) созданной записи из БД.

47. Какое условие обязательно для подключения устройства к Android Studio по Wi-Fi?
Телефон и ПК должны быть в одной сети

48. Какой модификатор используется для задания скругленной формы кнопке, например, с 50% радиусом?
shape

49. Какой архитектурный подход, упомянутый в лекции, подразумевает разделение приложения на Model, View и ViewModel?
MVVM

50. Какая функция из Animation API используется для создания анимации плавного изменения значения размера (в dp), например, для движения объекта?
animateDpAsState

51. Какое свойство в `graphicsLayer` отвечает за угол поворота компонента?
rotationZ

52. Что в ViewModel используется для хранения временного, изменяемого состояния, связанного с UI, например, текста в поле ввода?
mutableStateOf

53. Какой метод (или ключевое слово) используется в тестовых функциях для проверки, что ожидаемый результат соответствует фактическому (например, что найденный объект не равен null)?
assert

54. Какой файл нужно настроить для включения режима отладки приложения?
build.gradle.kts

55. Основное назначение модификатора weight внутри контейнеров Row или Column – это:
Пропорционально распределить свободное пространство между дочерними элементами

56. Для чего в MainActivity передается applicationContext в корневые Composable-функции (Main, FirstScreen и т.д.)?
Для корректной работы функций, требующих контекста, например, всплывающего сообщения (Toast).

57. Какой параметр функции `tween()` используется для установки времени выполнения анимации в миллисекундах?
durationMillis

58. Какой компонент библиотеки Room представляет собой таблицу в базе данных?
Entity

59. Какой компонент Compose **обязательно** используется для преобразования  `LiveData<List< ... >> `  из ViewModel в состояние, которое может быть использовано в Composable-функции?
observeAsState()

60. В какой папке проекта должны быть размещены ассеты (файлы) иконки приложения после их генерации?
res/mipmap

61. Какой инструмент в Android Studio используется для просмотра системных и пользовательских логов?
Logcat

62. Какой модификатор позволяет задать как минимальный, так и максимальный размер объекта приложения?
sizeIn()

63. Какой объект объявляется в функции Main и затем передается в BottomNavigationBar для контроля перемещений между экранами?
NavController

64. При изменении какого параметра функции `animate*AsState` запускается анимация?
targetValue

65. Какая аннотация используется для обозначения класса, который является таблицей в базе данных Room?
@Entity

66. Какой компонент Compose наиболее эффективен для вывода большого, прокручиваемого списка данных (например, всех записей из таблицы БД)?
LazyColumn

67. Какой режим сборки (`Build Variant`) предназначен для финальной версии приложения, готовой к публикации или распространению?
release

68. Какой тип точки останова позволяет остановить выполнение программы при возникновении ошибки?
Исключения

69. Какое свойство в Jetpack Compose чаще всего используется для получения ширины экрана устройства в единицах dp (Density-independent Pixels)?
LocalConfiguration.current.screenWidthDp

70. Какой из перечисленных способов является самым примитивным примером передачи данных (ресурсов) между модулями, согласно лекции?
Импорт ресурсов, таких как изображения из папки R.drawable

71. Какой параметр функции `tween()` позволяет контролировать кривую скорости анимации (например, медленный старт и быстрое завершение)?
easing

72. Какой компонент Room является интерфейсом, содержащим методы для выполнения операций с БД (запросы, вставка, удаление)?
DAO

73. Какой класс в навигации используется для определения строковых констант, которые служат маршрутами перехода между экранами?
sealed class NavRoutes

74. Какой тип установочного файла является **рекомендуемым** для публикации в Google Play Store?
`.aab` (Android App Bundle)

 75. В чем состоит основное преимущество использования weight вместо жестких width/height для создания адаптивного макета?
 weight делает макет более гибким, позволяя ему растягиваться/сжиматься.

76. Что представляет собой View в архитектуре MVVM?
Frontend, то, с чем взаимодействует пользователь.

77. Какая функция, используемая в `animationSpec`, позволяет добавить анимации эффект пружины?
spring

78. Какая аннотация используется для обозначения **первичного ключа** в классе Entity?
@PrimaryKey

79. Как Composable-функция (View) получает возможность **изменить** данные в БД (например, добавить запись)?
UI вызывает публичную функцию из ViewModel, которая делегирует вызов Репозиторию.

80. Какой пункт меню в Android Studio используется для запуска процесса создания готового APK-файла?
Build -> Build App Bundle(s) / APK(s) -> Build APK(s)

81. Назовите два инструмента Android Studio, которые используются при пошаговой отладке приложения.
Debug Logcat